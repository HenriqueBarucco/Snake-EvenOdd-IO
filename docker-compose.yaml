version: "3"

services:
  app:
    image: node:16
    volumes:
      - ./game:/app
    working_dir: /app
    command: ["npm", "start"]
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production